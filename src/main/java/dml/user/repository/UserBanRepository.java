package dml.user.repository;

import dml.common.repository.CommonRepository;
import dml.user.entity.UserBan;

public interface UserBanRepository<E extends UserBan, ID> extends CommonRepository<E, ID> {
}
